"use client";import {MapContainer,Marker,Polyline,TileLayer,useMap} from "react-leaflet";import L from "leaflet";import "leaflet/dist/leaflet.css";import {useEffect} from "react";const icon=L.divIcon({className:"planeMarker",html:"<span>✈</span>",iconSize:[36,36],iconAnchor:[18,18]});function Follow({live}:{live:any}){const map=useMap();useEffect(()=>{if(live?.latitude!=null&&live?.longitude!=null)map.flyTo([Number(live.latitude),Number(live.longitude)],6,{duration:.7})},[live,map]);return null}export default function RouteMap({live}:{live:any}){const man:[number,number]=[53.365,-2.272],snn:[number,number]=[52.702,-8.924],pos=live?.latitude!=null&&live?.longitude!=null?[Number(live.latitude),Number(live.longitude)] as [number,number]:null;return <div className="map"><MapContainer center={pos||[53.05,-5.6]} zoom={6} scrollWheelZoom zoomControl><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Polyline positions={pos?[man,pos,snn]:[man,snn]} pathOptions={{color:"#007aff",weight:4,dashArray:pos?undefined:"7 8"}}/>{pos&&<Marker position={pos} icon={icon}/>}<Follow live={live}/></MapContainer></div>}
